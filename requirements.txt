# =============================================================================
# ServiceUP Core Dependencies
#
# Список сверен с реальным использованием в коде (grep import/from по всей
# кодовой базе) — раньше здесь было ~50 пакетов, большинство из которых
# нигде не импортировались (redis, celery, fastapi, uvicorn, alembic,
# asyncpg, aiosqlite, transitions, aiosmtplib, injector, dynaconf, loguru,
# structlog, pyjwt, passlib, cachetools, aioredis, tenacity, arrow,
# factory-boy, ttkbootstrap, python-barcode, orjson, dependency-injector,
# httpx, aiohttp, fpdf2, python-dotenv, bcrypt, cryptography) — раздували
# установку (pip install -r requirements.txt в start.bat) без всякой пользы.
# bcrypt/passlib вернутся, когда реально начнётся TODO_RBAC_ROADMAP.md
# (хэширование паролей) — сейчас там только план, кода ещё нет.
# См. AUDIT_REPORT_v25.md.
# =============================================================================

# -----------------------------------------------------------------------------
# GUI Framework
# -----------------------------------------------------------------------------
customtkinter>=5.2.0
Pillow>=10.0.0

# -----------------------------------------------------------------------------
# PDF Generation & Document Processing
# -----------------------------------------------------------------------------
reportlab>=4.0.0
pypdfium2>=4.0.0

# -----------------------------------------------------------------------------
# HTTP & Networking
# -----------------------------------------------------------------------------
requests>=2.31.0

# -----------------------------------------------------------------------------
# Web Server (PWA — мобильная версия, pwa/server.py)
# -----------------------------------------------------------------------------
flask>=3.0.0

# -----------------------------------------------------------------------------
# QR Codes (PWA — QR для открытия мобильной версии)
# -----------------------------------------------------------------------------
qrcode>=7.4.0

# -----------------------------------------------------------------------------
# Data Validation & Settings (config/settings.py)
# -----------------------------------------------------------------------------
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Опционально: используется только если установлен (try/except в
# utils/validators.py) — не критично для запуска без него.
phonenumbers>=8.13.0

# -----------------------------------------------------------------------------
# Database & ORM
# -----------------------------------------------------------------------------
sqlalchemy>=2.0.0

# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# -----------------------------------------------------------------------------
# Development Tools (CLI, не импортируются из кода — см. pyproject.toml)
# -----------------------------------------------------------------------------
ruff>=0.1.0
mypy>=1.8.0
