"""database.facade — Database (SQLAlchemy-facade) разбита на mixin'ы по
разделу ответственности, чтобы не держать один файл на 1400+ строк
(см. AUDIT_REPORT_v25.md, Task T).

Каждый файл здесь — один mixin (устройства/словари/вычисления/запросы/
блокировки/финансы/дочерние таблицы/клиенты), в точности как раньше был
один "# ==== РАЗДЕЛ ====" внутри одного класса — просто теперь раздел это
файл. Итоговый класс Database (database/sqlalchemy_database.py) собирает
их через множественное наследование, тем же способом, каким core/base.py
уже собирает BaseService/BaseRepository из LoggableMixin/
ExceptionHandlingMixin/... — ни один внешний вызов self.db.method(...) не
меняется, потому что имя и сигнатура каждого метода остаются ровно теми
же, просто физически лежат в другом файле.

shared.py содержит то, чем пользуются НЕСКОЛЬКО mixin'ов (свободные
функции/константы) — не сам mixin, обычный модуль."""
